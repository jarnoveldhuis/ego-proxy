<!DOCTYPE html>

<html lang="en">

<head>
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-FJPMPMZGB2"></script>
  <script>
    window.dataLayer = window.dataLayer || [];

    function gtag() {
      dataLayer.push(arguments);
    }
    gtag('js', new Date());
    gtag('config', 'UA-76345573-1');
    gtag('config', 'G-FJPMPMZGB2');
  </script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

  <meta charset="UTF-8">
  <!-- <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no"> -->
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="output.css" rel="stylesheet">
  <!-- Primary Meta Tags -->
  <title><%= Object.keys(proxies)[0] %> Proxy</title>
  <meta name="title" content="Ego-Proxy" />
  <meta name="description" content="Talk to <%= Object.keys(proxies)[0] %>." />

  <!-- Open Graph / Facebook -->

  <meta property="og:image" content="<%= proxies[Object.keys(proxies)[0]].joy[0].url %>" />
  <meta property="og:type" content="website" />
  <meta property="og:title" content="Ego-Proxy" />
  <meta property="og:description" content="Talk to <%= Object.keys(proxies)[0] %>." />
  <meta property="twitter:image" content="<%= proxies[Object.keys(proxies)[0]].joy[0].url %>" />
  <link rel="icon" href="/img/proxicon.ico" sizes="any" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">

</head>

<body>
  <div id="full-container">
    <div id="chat-container">
      <div id="prompt"></div>
      <div class="chat speech bottom text-with-border response-container" id="response-container">
        <div class="speech-content">
          <div id="botResponse"></div>
          <div id="audioControls" style="display:none;">
            <i class="fas fa-play audio-control-icon" id="playIcon" title="Replay"></i>
            <i class="fas fa-download audio-control-icon" id="downloadIcon" title="Download"></i>
          </div>
          <div id="voiceLoad"></div>
        </div>
      </div>
      <div class="bot-container" style="background-image: url('/img/guest.png');">
        <img id="botImage" src="/img/guest.png" alt="proxi" />
      </div>

      <div class="input-container">
        <form id="ask" autocomplete="off" action="/ask/" onsubmit="askBot(event); gtag('event', 'submission', {'event_category': 'Chat', 'event_label': 'AskBot'}); return false;">
          <div class="submitAs">
            <div class="input-group input-group-sm mb-3">
              <div class="input-group-prepend">
                <select class="form-select" aria-label=" select" id="submitAs" name="as">
                  <% if (context.submitAsOptions && context.context !=='Custom' ) { %>
                  <% context.submitAsOptions.forEach(function(option) { %>
                  <option value="<%= option %>">
                    <%= option %>
                  </option>
                  <% }); %>
                  <% } %>
                </select>
              </div>
              <br>
              <textarea class="userInput form-control" id="userInput" type="text" name="name" rows="1" placeholder="Say something..."></textarea>
            </div>
          </div>

          <div class="d-flex justify-content-between align-items-center">
            <div>
              <button class="btn" id="ttsButton" type="button" aria-label="Toggle sound">
                <i class="fas fa-volume-mute" id="ttsIcon"></i>
              </button>
              <button class="btn" type="button" data-bs-toggle="modal" data-bs-target="#feedbackModal">
                <i class="fas fa-envelope"></i>
              </button>
              <% if (context.alias !== "CT" ) { %>
              <button class="btn toggle-button" id="toggleButton" type="button" data-bs-toggle="modal" data-bs-target="#settingsModal" display="block">
                <i class="fas fa-cog"></i>
              </button>
              <% } %>
            </div>

            <div class="d-flex submit-prompt align-items-center">
              Submit to:&nbsp;
              <span id="newButtonPlaceholder"></span>
              <div class="btn-group" id="submitTo" role="group">
                <% if (context.submitToOptions && context.context !=='Custom' ) { %>
                <% context.submitToOptions.forEach(function(option) { %>
                <input class="btn btn-rounded btn-outline-dark btn-sm" name="go" type="submit" value="<%= option %>" data-tag="button-<%= option %>" style=" background-color: black; color: white; border-color: white; outline-color: white;">
                <% }); %>
                <% } %>
              </div>
            </div>
          </div>
      </div>
      </form>
    </div>
  </div>
  </div>

  <!-- Success Alert -->

  <% if (context.alias !== "CT" ) { %>
  <!-- Settings Modal -->
  <div class="modal fade" id="settingsModal" tabindex="-1" aria-labelledby="hide" data-bs-backdrop="static" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="settingsModalLabel">Menu</h5>
          <!-- <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button> -->
        </div>



        <!-- Nav tabs -->
        <!-- <div id="navTabs">
          <ul class="nav nav-tabs" id="myTab" role="tablist">
            <li class="nav-item" role="presentation">
              <button class="nav-link active" id="profile-tab" data-bs-toggle="tab" data-bs-target="#profile" type="button" role="tab" aria-controls="profile" aria-selected="false">Profile</button>
            </li>
            <li class="nav-item" role="presentation">
              <button class="nav-link" id="share-tab" data-bs-toggle="tab" data-bs-target="#share" type="button" role="tab" aria-controls="share" aria-selected="true">Share</button>
            </li> -->


        <!-- <li class="nav-item" role="presentation">
              <button class="nav-link" id="custom-tab" data-bs-toggle="tab" data-bs-target="#custom" type="button" role="tab" aria-controls="custom" aria-selected="true">Add Proxies</button>
            </li> -->
        <!-- <li class="nav-item" role="presentation">
                <button class="nav-link" id="custom-tab" data-bs-toggle="tab" data-bs-target="#meet" type="button" role="tab" aria-controls="custom" aria-selected="true">Meet</button>
              </li> -->
        <!-- </div id="navTabs"> -->

        <div class="modal-body">
          <form>
            <!-- Dropdown to select context -->
            <div id="allContent" style="display: block">
              <div class="mb-3">
                <label for="contextSelect"><b>Select Context:</b></label>

                <select class="form-select custom-select" id="contextSelect" onchange="updateContext(); updateUrl(this.value.toLowerCase().toString())">
                  <option value="Settings" disabled>Select...</option>
                  <option value="Meet" selected <% if (context.context === 'meet') { %>selected<% } %>>Meet</option>
                  <option value="Interview" <% if (context.context === 'Interview') { %>selected<% } %>>Interview</option>
                  <option value="Date" <% if (context.context === 'Date') { %>selected<% } %>>Date</option>
                  <option value="Debate" <% if (context.context === 'Debate') { %>selected<% } %>>Debate</option>
                </select>
                <small id="contextDescription" class="form-text text-muted">Update manually or click the train button to refine.</small>
              </div>
              
              <div class="d-flex justify-content-between align-items-center mb-2">
                <div>
                  <label for="contentField" id="contentId">Profile</label>
                  <br>
                  <small class="form-text text-muted">Click practice to refine your profile automatically.</small>
                </div>
              
                <div class="d-flex">
                  
                  <button type="button" class="btn btn-outline-dark btn-sm" id="repeatTraining" data-toggle="tooltip" data-placement="left" title="Practice to refine your profile." onclick="train()">Practice
                    <i class="fas menuIcon fa-chalkboard-teacher"></i>
                  </button>
                  <button class="btn btn-outline-dark btn-sm ml-2" id="save" disabled onclick="updateProxy(event)">
                    <i class="fas menuIcon fa-save"></i>
                  </button>
                </div>
              </div>

              <form id="editForm" action="/update-proxy" method="post">
                <input type="hidden" id="contentIdField" name="contentId" value="">
                <div class="mb-3">
                  <textarea class="form-control" id="contentField" name="content" rows="3" placeholder="Click 'Train' below to a practice interview and autogenerate your proxy's profile or just update manually here."></textarea>
                </div>
              </form>
              <!-- <button type="button" class="btn btn-primary" id="train" onclick="tryShare()">Train</button> -->
              <!-- <div class="modal-footer">
                <button type="button" class="btn btn-primary" style="display:none" id="meetProxy" onclick="updateProxy(), meet('share')">
                  <%= Object.keys(proxies)[0] %> <%= Object.keys(proxies)[0] %>
                </button>
                <button class="btn btn-primary" id="save" disabled onclick="updateProxy()">
                  Save
                </button>
                <button type="button" class="btn btn-primary" id="trainProxy" data-toggle="tooltip" data-placement="left" title="Tooltip on left" onclick="updateProxy(), train()">
                  Train <%= Object.keys(proxies)[0] %>
                </button> -->
              <!-- <small class="text-muted ms-2">Refines your profile with each practice</small> -->
              <!-- <span class="ms-2" tabindex="0" data-bs-toggle="popover" data-bs-trigger="focus" title="Practice Debates" data-bs-content="Refines your profile with each practice">
                  <i class="bi bi-info-circle"></i>
                </span>
              </div> -->


              <div id="errorMessage" class="alert alert-danger" role="alert" style="display: none;">
                Update Failed
              </div>


            </div>
            <!-- Tab panes -->
            <div class="tab-content" id="myTabContent">
              <div class="tab-pane fade" id="custom" role="tabpanel" aria-labelledby="custom-tab">
                <!-- Guest Field -->
                <div class="dropdown">
                  <button class="btn btn-outline-dark dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Add Guests
                  </button>
                  <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                    <div class="public-proxies dropdown-header"><b>Public Proxies:</b></div>
                    <% if (publicProxies.length > 0) { %>
                    <% publicProxies.forEach(function(option) { %>
                    <a class="dropdown-item" id="<%= option %>Button" onClick="updateUrl(document.getElementById('contextSelect').value.toLowerCase())">
                      <%= option %>
                      <% }); %>
                    </a>
                    <% } else { %>
                    <p>No public proxies available.</p>
                    <% } %>
                    <div class="dropdown-divider"></div>
                    <h6 class="your-proxies dropdown-header"><b>Your Other Proxies:</b></h6>
                    <% if (yourProxies.length > 0) { %>
                    <% yourProxies.forEach(function(option) { %>
                    <a class="dropdown-item" id="<%= option %>Button" onClick="addButton(this, '<%= option %>'),updateUrl(document.getElementById('contextSelect').value.toLowerCase())">
                      <%= option %>
                      <% }); %>
                    </a>
                    <% } else { %>
                    <a class="dropdown-item" disabled>None Available</a>
                    <% } %>
                    <!-- <div class="dropdown-divider"></div>
                    <a class="dropdown-item" id="createProxyLink" href="#" target="_blank"><b>Create New Proxy</a></b></a> -->

                  </div>
                </div>


              </div>
              <!-- Training Field -->
              <!-- <div id='trainer' class="form-group row">
                <div class="input-group mb-3">
                  <div class="input-group-prepend">
                    <span class="input-group-text" id="trainingContext">Practice:</span>
                  </div> -->
              <!-- <select class="custom-select" id="proxySelect" onchange="updateUrl(document.getElementById('contextSelect').value.toLowerCase())">
                    <optgroup label="Public Proxies">
                      <% if (publicProxies.length > 0) { %>
                      <% publicProxies.forEach(function(option) { %>
                      <option value="<%= option %>"><%= option %></option>
                      <% }); %>
                      <% } else { %>
                      <option disabled>No public proxies available.</option>
                      <% } %>
                    </optgroup>
                    <optgroup label="Your Other Proxies">
                      <% if (yourProxies.length > 0) { %>
                      <% yourProxies.forEach(function(option) { %>
                      <option value="<%= option %>"><%= option %></option>
                      <% }); %>
                      <% } else { %>
                      <option disabled>None Available</option>
                      <% } %>
                    </optgroup>
                  </select> -->
              <!-- <div class="input-group-append">

                    <span class="input-group-text" id="trainProxy" data-toggle="tooltip" data-placement="left" title="Tooltip on left" onclick="updateProxy(), train()">
                      as <%= Object.keys(proxies)[0] %>
                    </span>

                  </div>

                </div>
              </div> -->
              <!-- Share Tab -->
              <!-- <div class="tab-pane fade " id="share" role="tabpanel" aria-labelledby="share-tab"> -->


              <!-- Dependent Fields -->

              <!-- Interview Fields -->
              <div id="interviewModal" style="display: none;">
                <div class="form-group row">
                  <div id='role' class="col">
                    <label for="roleInput" class="col-form-label col-form-label-sm col" style='font-weight: bold;'><b>Open Role:</b></label>
                    <div class="input-group input-group-sm mb-3 col">
                      <input type="text" id="roleInput" class="params form-control" placeholder="Enter role" onkeypress="return alpha(event)" oninput="updateUrl('interview')">
                    </div>
                  </div>
                  <div id='org' class="col">
                    <label for="orgInput" class="col-form-label col-form-label-sm" style='font-weight: bold;'><b>Organization:</b></label>
                    <div class="input-group input-group-sm mb-3">
                      <input type="text" id="orgInput" class="params form-control" placeholder="Enter org" onkeypress="return alpha(event)" oninput="updateUrl('interview')">
                    </div>
                  </div>
                </div>
              </div>

              <!-- Date Fields -->
              <div id="dateModal" style="display: none;">
              </div>

              <!-- Debate Fields -->
              <div id="debateModal" style="display: none;">
                <div id='topic' class="form-group row">
                  <div class="col">
                    <div class="input-group-sm">
                      <label for="topicInput" class="col-form-label col-form-label-sm col" style='font-weight: bold;'><b>Topic:</b></label>

                      <div class="input-group-sm input-group-sm mb-3">
                        <input type="text" id="topicInput" class="params form-control" placeholder="Enter topic" onkeypress="return alpha(event)" oninput="updateUrl('debate')">
                      </div>
                    </div>
                  </div>
                  <div class="dropdown col">

                  </div>
                </div>

              </div>
              <!-- Name Fields -->
              <!-- <div id='name' class="form-group row">
                  <div class="col">
                    <div class="input-group-sm">
                      <label id='yourName' class="col col-form-label col-form-label-sm col" style='font-weight: bold;'><b>Who will this proxy speak with?</b></label>

                      <div class="input-group-sm input-group mb-3">
                        <input type="text" id="nameInput" class="params form-control" placeholder="Enter name" onkeypress="return alpha(event)" oninput="updateUrl(context.context.toLowerCase())" pattern="[A-Za-z\s]+" title="Only letters and spaces are allowed">
                      </div>
                    </div>
                  </div>
                  <div class="dropdown col">

                  </div> -->

              <!-- Standard Fields -->
              <div id="standardModal">
                <div class="form-group row">

                  <div id='name' class="col">
                    <label for="nameInput" id='yourName' class="col-form-label col-form-label-sm" style='font-weight: bold;'><b>Name:</b></label>
                    <div role="group" aria-label="Basic checkbox toggle button group" id="hostButtons">
                    </div>
                    <div class="input-group input-group-sm mb-3" id="nameInputContainer">
                      <input type="text" id="nameInput" class="params form-control" placeholder="Enter name" onkeypress="return alpha(event)" oninput="updateUrl(context.context.toLowerCase())" pattern="[A-Za-z\s]+" title="Only letters and spaces are allowed">
                    </div>
                  </div>
                  <div id='guests' class="col">
                    <!-- <label for="guestInput" class="col-form-label col-form-label-sm col" style='font-weight: bold;'><b>Guest Names </b></label> -->
                    <label for="dropdownMenuButton" id="guestProxies" class="col-form-label col-form-label-sm col" style='font-weight: bold;'><b>Guest Proxies:</b></label><br>
                    <!-- <b id="guestProxies" style="display: block;">Guest Proxies:</b> -->
                    <button class="btn btn-sm btn-outline-dark dropdown-toggle dropdown-toggle-split" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                      Add Guest
                    </button>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                      <h6 class="public-proxies dropdown-header"><b>Public Proxies:</b></h6>
                      <% if (publicProxies.length > 0) { %>
                      <% publicProxies.forEach(function(option) { %>
                      <a class="dropdown-item" id="<%= option %>Button" onClick="addButton(this, '<%= option %>'), updateUrl(document.getElementById('contextSelect').value.toLowerCase())">
                        <%= option %>
                        <% }); %>
                      </a>
                      <% } else { %>
                      <p>No public proxies available.</p>
                      <% } %>
                      <div class="dropdown-divider"></div>
                      <h6 class="your-proxies dropdown-header"><b>Your Other Proxies:</b></h6>
                      <% if (yourProxies.length > 0) { %>
                      <% yourProxies.forEach(function(option) { %>
                      <a class="dropdown-item" id="<%= option %>Button" onClick="addButton(this, '<%= option %>'),updateUrl(document.getElementById('contextSelect').value.toLowerCase())">
                        <%= option %>
                        <% }); %>
                      </a>
                      <% } else { %>
                      <a class="dropdown-item" disabled>None Available</a>
                      <% } %>
                      <div class="dropdown-divider"></div>
                      <a class="dropdown-item" id="createProxyLink" href="#" target="_blank"><b>Create New Proxy</a></b></a>
                    </div>
                  </div>
                </div>
              </div>


              <!-- <button class="btn btn-sm btn-outline-primary dropdown-toggle dropdown-toggle-split" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Add Guest
              </button> -->



              <!-- Shareable Fields -->
              <div id="allUrl" style="display: block">

                <div id='url'><b>Shareable URL:</b></div>
                <div id='name' class="form-group row">
                  <div class="input-group input-group-sm mb-3">
                    <!-- <div class="input-group-prepend">
                      <span class="input-group-text" id='yourName'>Name:</span>
                    </div> -->



                    <!-- <button type="button" id="urlCopy" onclick="copyUrl()" class="btn btn-outline-primary" data-bs-toggle="tooltip" data-bs-placement="top" title="Copied!"><i class="fas fa-share"></i></button> -->


                  </div>
                </div>
              
              <div class="input-group url input-group-sm mb-3">
                <input type="text" id="urlInput" class="form-control col-10" readonly>
                <button type="button" id="urlCopy" onclick="copyUrl()" class="btn btn-outline-dark" data-bs-toggle="tooltip" data-bs-placement="top" title="Copied!">Copy</button>

              </div>
              </div>
            </div>



            <div class="modal-footer">

              <!-- <button type="button" class="btn btn-primary" style="display:none" id="meetProxy" onclick="updateProxy(), meet('share')">
                  <%= Object.keys(proxies)[0] %> <%= Object.keys(proxies)[0] %>
                </button> -->

              <button type="button" class="btn btn-dark" id="meetProxy" onclick="redirectToUrl(regularUrl)">
                Meet <%= Object.keys(proxies)[0] %>
              </button>

              <button type="button" class="btn btn-outline-primary" id="beginTraining" data-toggle="tooltip" data-placement="left" title="Tooltip on left" onclick="train()">
                Train <%= Object.keys(proxies)[0] %>
              </button>
              <!-- <button class="btn btn-primary" id="save" disabled onclick="updateProxy()">
                    Save
                  </button> -->


              <span class="ms-2" tabindex="0" data-bs-toggle="popover" data-bs-trigger="focus" title="Practice Debates" data-bs-content="Refines your profile with each practice">
                <i class="bi bi-info-circle"></i>
              </span>

              <!-- <button type="button" class="btn btn-primary" id="begin" onclick="redirectToUrl(regularUrl)">Meet <%= Object.keys(proxies)[0] %></button>
                  <button type="button" class="btn btn-primary" style="display:none" id="backToProfile" onclick="backToProfile()">Cancel</button> -->
            </div>




            <!-- <button type="button" class="btn btn-primary" id="begin" onclick="redirectToUrl(regularUrl)">Meet <%= Object.keys(proxies)[0] %></button>
                  <button type="button" class="btn btn-primary" style="display:none" id="backToProfile" onclick="backToProfile()">Cancel</button> -->

            <!-- <div id='url'><b>Shareable URL:</b></div>
                <div class="input-group url input-group-sm mb-3">
                  <input type="text" id="urlInput" class="form-control col-10" readonly>
                  <button type="button" id="urlCopy" onclick="copyUrl()" class="btn btn-outline-primary" data-bs-toggle="tooltip" data-bs-placement="top" title="Copied!">Copy</button>
                </div> -->
            <!-- </div> -->

            <!---Profile Tab-->
            <!-- <div class="tab-pane fade  active show" id="profile" role="tabpanel" aria-labelledby="profile-tab"> -->
            <!-- Content Field -->
            <!-- <button type="button" class="btn btn-primary btn-sm" id="save" disabled onclick="updateProxy()"></button> -->


            <!-- </div> -->

            <div id="proxyCreated" class="alert alert-success" role="alert" style="display: none;">
              <div id='Intro'>
                <%= training ? 'Meet ' + Object.keys(proxies)[0]  +'!' : "Your proxy's Avatar has been generated! It's time to train " + Object.keys(proxies)[0]  + " to inhabit it's new identity." %>
              </div>
            </div>


        </div>

        <style>

        </style>
      </div>

      <div class='links'>
      </div>
      </form>

    </div>
    <div id="contextUpdated" class="alert alert-success alert-dismissible fade" role="alert" style="display: none;">
      <h4 class="alert-heading">Personality Updated!</h4>
      <hr> Use this menu to:


      <p class="mb-0">
      <ul>
        <li>Review and refine <%= Object.keys(proxies)[0] %>'s personality.</li>
        <li>Create custom shareable links.</li>
        <li>Train <%= Object.keys(proxies)[0] %> to interact on your behalf in other contexts.</li>
      </ul>
      Click the cog icon to access this menu when you need it.</p>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  </div>
  </div>
  </div>
  <% } %>
  </div>

  <!-- Feedback Modal -->
  <div class="modal fade" id="feedbackModal" tabindex="-1" aria-labelledby="feedbackModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="feedbackModalLabel">Feedback</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form id="feedbackForm">
            <div class="mb-3">
              <label for="feedbackText" class="col-form-label">Your Feedback:</label>
              <textarea class="form-control" id="feedbackText"></textarea>
            </div>
            <div class="mb-3">
              <label for="proxyEmail" class="form-label">Email address</label>
              <input type="email" class="form-control" id="proxyEmail" placeholder="name@example.com">
            </div>
            <button type="submit" class="btn btn-primary">Send Feedback</button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- TTS Threshold Modal -->
  <div class="modal" tabindex="-1" id="rateLimitModal">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Notice</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <p>You've reached the maximum number of Text to Speech requests. Try again later. If you're enjoying
            the app, consider supporting it via Patreon.</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Nah</button>
          <button type="button" class="btn btn-primary" id="patreonButton">Support on Patreon</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Transcript Modal -->
  <div class="modal fade" id="transcriptModal" tabindex="-1" aria-labelledby="transcriptModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="transcriptModal">Transcript</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body" id="transcript"></div>
      </div>
    </div>
  </div>

</body>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>

<script>
  const context = <%- JSON.stringify(context) %>;
  console.log("submitTo", context.submitToOptions)
  console.log("submitAs", context.submitAsOptions)
  const proxies = <%- JSON.stringify(proxies) %>;
  console.log("proxies", proxies)
  const publicProxies = <%- JSON.stringify(publicProxies) %>;
  const yourProxies = <%- JSON.stringify(yourProxies) %>;
  const proxyName = Object.keys(proxies)[0]
  const transcriptThreshold = <%- JSON.stringify(transcriptThreshold) %>;
  const hasShareParam = <%- JSON.stringify(hasShareParam) %>;
</script>

<script src="js/chat.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>

<script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.3.3/html2canvas.min.js"></script>


</html>